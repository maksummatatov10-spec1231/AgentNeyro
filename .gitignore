# --- Godot: кэш импорта (НЕ коммитим), но *.import коммитим (настройки импорта) ---
.godot/

# --- Временные распакованные/анализные папки ---
_analysis/
assets_download/
_extracted/

# --- OS / редакторы ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- Сборки/экспорт ---
build/
*.exe
*.x86_64
*.app
# PCK экспорта не коммитим (генерируется), но ВЕЩИ в проекте — коммитим:
# export_presets.cfg обычно коммитят — оставляем.
